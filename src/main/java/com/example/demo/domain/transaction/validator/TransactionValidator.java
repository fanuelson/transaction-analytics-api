package com.example.demo.domain.transaction.validator;

import com.example.demo.domain.common.validator.Validator;
import com.example.demo.domain.transaction.Transaction;

public interface TransactionValidator extends Validator<Transaction> {
}
